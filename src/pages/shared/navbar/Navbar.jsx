import Logo from "../../../components/logo/Logo";

const Navbar = () => {
  return (
    <nav className="custom-container mx-auto py-4">
      <Logo></Logo>
    </nav>
  );
};

export default Navbar;
